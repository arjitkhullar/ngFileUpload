## This project uses [Multer](https://github.com/expressjs/multer) to parse files on the backend and [angular-file-upload](https://github.com/nervgh/angular-file-upload/wiki/Module-API) on the frontend.

## Use `npm run start` to deploy the application on port 3000.

